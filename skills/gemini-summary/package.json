{
  "name": "gemini-summary-skill",
  "version": "1.0.0",
  "description": "统一摘要 skill — 自动侦测 URL/PDF/影片并产生繁体中文 Markdown 摘要",
  "type": "module",
  "scripts": {
    "build": "npx -y bun build src/summarize.js --outfile scripts/summarize.js --target node --minify",
    "summarize": "node scripts/summarize.js"
  },
  "dependencies": {
    "@google/genai": "^2.0.0",
    "@mozilla/readability": "^0.6.0",
    "linkedom": "^0.18.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
