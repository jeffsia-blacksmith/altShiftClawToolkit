{
  "name": "gemini-image-describer-skill",
  "version": "1.0.0",
  "description": "图片内容描述工具 — 使用 Gemini API 分析图片并产生繁中描述",
  "type": "module",
  "scripts": {
    "build": "npx -y bun build src/describe.js --outfile scripts/describe.js --target node --minify",
    "describe": "node scripts/describe.js"
  },
  "dependencies": {
    "@google/genai": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
