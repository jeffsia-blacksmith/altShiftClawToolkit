{
  "name": "gemini-audio-transcriber-skill",
  "version": "1.0.0",
  "description": "音讯转繁中逐字稿工具 — 使用 Gemini Interactions API 分析音讯并产生繁中逐字稿",
  "type": "module",
  "scripts": {
    "build": "npx -y bun build src/transcribe.js --outfile scripts/transcribe.js --target node --minify",
    "transcribe": "node scripts/transcribe.js"
  },
  "dependencies": {
    "@google/genai": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
